function Job({ salary, position, company }) {
  return (
    <div>
      <h1>Salary: {salary}</h1>
      <h2>Position: {position}</h2>
      <h2>Company: {company}</h2>
    </div>
  );
}

export default Job;
